# Routes Workspace

This folder is reserved for backend route-layer organization as the CBC learning platform grows.

- `controllers/` – request handlers and route controller modules.
- `middleware/` – reusable request middleware such as auth, validation, and error handling.
- `config/` – route-specific configuration helpers.
- `uploads/` – local upload staging for development-time file handling.
- 
